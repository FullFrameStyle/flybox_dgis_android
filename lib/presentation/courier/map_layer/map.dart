import 'package:flutter/material.dart';

import 'main.dart';

class MapWidget extends StatelessWidget {
  const MapWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const GisScreen();
  }
}
